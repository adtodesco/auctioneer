version: "3.8"

services:
  web:
    build: .
    ports:
      - 8000:8000
    volumes:
      - sqlite-instance:/auctioneer/instance
  nginx:
    build: ./nginx
    ports:
      - 80:80
    depends_on:
      - web

volumes:
  sqlite-instance: