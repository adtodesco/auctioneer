version: "3.8"

services:
  web:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - sqlite-instance:/auctioneer/instance
  nginx:
    build: ./nginx
    ports:
      - 443:443
    depends_on:
      - web

volumes:
  sqlite-instance: